ALERT Memory5
  IF node_memory_MemFree / node_memory_MemTotal * 100 < 5
  FOR 5m
  LABELS {
    severity="warning",
    type="memory-usage",
    type="meta-failure"
  }
  ANNOTATIONS {
    summary = "",
    description = "Memory percentage free is {{ $value }} < 5%"
  }